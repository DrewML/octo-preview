{
  "name": "octo-preview",
  "description": "Live preview for markdown comments in PRs/Issues on GitHub",
  "scripts": {
    "build-chrome": "./scripts/build-chrome",
    "zip": "zip -jr chrome.zip dist/chrome/",
    "release": "npm run build-chrome && npm run zip && webstore upload --file chrome.zip --auto-publish"
  },
  "author": "Andrew Levine",
  "devDependencies": {
    "chrome-webstore-upload-cli": "^0.2.0",
    "babel-core": "^6.3.15",
    "babel-preset-es2015": "^6.3.13",
    "babelify": "^7.2.0",
    "browserify": "^11.2.0"
  }
}
